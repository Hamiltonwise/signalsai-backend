import React from "react";

/**
 * GoogleAccountStatus — placeholder for showing connected Google account info.
 * The old version depended on the Google OAuth login context which has been removed.
 * This will be rebuilt in Plan 07 (Settings GBP Only) to show GBP connection status
 * based on the organization's google_connections record.
 */
export const GoogleAccountStatus: React.FC = () => {
  // Will be rebuilt with proper GBP connection status in Plan 07
  return null;
};
